export interface SkillListItem {
  skillID: string;
  title: string;
  type: string;
  createDate: Date;
}

export interface SkillDetails {
  skillID: string;
  title: string;
  description: string;
  type: number;
}
